# Workspace Summary

## Purpose

This file summarizes the current repository status in a controlled format for Ollama.
It is intended to support disciplined project awareness.

## Current Status

Based on current repository evidence:

- `zmos-core` is initialized as a minimal Node.js project
- `package.json` exists and declares ES module mode
- the repository has the planned top-level structure for Z-MOS core work
- most core implementation areas remain scaffolding and are not yet implemented
- Ollama governance preparation has been added under `ai/ollama/`
- no AI runtime code has been added yet

## Governance Interpretation

At the current stage, the repository is in governance and workspace preparation rather than active runtime integration.
Ollama should treat this project as an early-phase scaffold with controlled context, not as a complete operational system.

## Usage Rule

This summary is advisory and must be updated from repository evidence when the workspace changes.
If current repository evidence does not support a claim, respond:

`not enough evidence`
