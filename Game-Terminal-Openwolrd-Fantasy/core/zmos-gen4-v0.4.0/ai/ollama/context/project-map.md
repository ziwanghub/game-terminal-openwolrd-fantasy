# Project Map

## Purpose

This file provides a controlled map of the current repository structure for Ollama.
It is a derived context artifact for project awareness.
It is not a source of truth.

## Current Top-Level Structure

Observed in the current repository:

- `ai/`: governed workspace for Ollama support materials
- `bin/`: intended CLI executable location
- `cli/`: intended command gateway and routing layer
- `contracts/`: intended shared contracts and schemas
- `core/`: intended core runtime and bootstrap logic
- `docs/`: documentation and Z-MOS planning materials
- `governance/`: intended governance logic and rules
- `scripts/`: intended helper or project scripts
- `tests/`: intended test location
- `trace/`: intended trace and evidence output layer
- `package.json`: minimal Node package definition

## Current Evidence Notes

- The repository has the intended top-level folder layout.
- Most implementation directories are currently empty.
- The repository contains governance preparation for Ollama in `ai/ollama/`.
- The main planning document is under `docs/zmos/`.

## Usage Rule

Use this file as a high-level orientation layer only.
If a required detail is not present here, consult approved evidence or respond:

`not enough evidence`
